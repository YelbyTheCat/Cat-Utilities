import React from 'react';
import PropTypes from 'prop-types';

const pages = ['Home', 'Jobs', 'Money']

import Button from 'react-bootstrap/Button';

const Header = () => {
  return (
    <div>
      Anything here
    </div>
  );
};


Header.propTypes = {

};


export default Header;
